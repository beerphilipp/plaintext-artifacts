# Measurement Scripts

Run one notebook after the other. The order matters, as one notebook may lay the groundworks for another.
The scripts expect a MongoDB running on `localhost:27017` with the populated (raw) results and access to the APK dataset. When run with `reproducibility/e3/e3.sh`, the script handles these requirements.
Please refer to the artifact appendix for a mapping of notebooks to sections in the paper.