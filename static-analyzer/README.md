# Static Analysis

The static analysis phase consist of a manifest analyzer `smanalyzer` and a bytecode analyzer `sootanalyzer`.

## Manifest Analyzer

Located in `smanalyzer`.

## Bytecode Analyzer

Located in `sootanalyzer`.